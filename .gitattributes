# Shell scripts and unit files run on Linux — force LF so a Windows committer
# can't ship CRLF that breaks bash / systemd on the deploy box.
*.sh            text eol=lf
*.service       text eol=lf
deploy/*.yml    text eol=lf
