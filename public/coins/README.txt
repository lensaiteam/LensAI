Drop real crypto logo PNGs here, named by lowercase ticker:

  btc.png   eth.png   sol.png   bnb.png
  xrp.png   doge.png  link.png  avax.png   pepe.png

Guidelines for best results:
- Square PNGs with a transparent background (the official brand
  icon on transparent works great). ~128–256px is plenty.
- The app shows them as circular chips and as large spotlight
  logos; transparent-bg icons sit cleanest on both light + dark.
- Until a file exists, the UI falls back to a colored glyph badge
  automatically — so the site works now and upgrades the moment
  you add the file (no code change needed).
