# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# env
.env
.env.local
.env*.local

# brand foundation preview served via dev only (canonical lives in brand/)
/public/foundation.html

# misc
.DS_Store
*.pem
_shot.mjs
Gemini_Generated_Image_*.png
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# typescript
*.tsbuildinfo

# capture layer (v2) — the immutable corpus is the moat; it is NEVER committed.
# It lives on the always-on capture host and is backed up off-machine (see README).
/data/
*.db
*.db-wal
*.db-shm
/backups/
